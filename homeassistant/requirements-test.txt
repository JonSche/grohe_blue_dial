# Reproduces the exact environment the M15 test suite was verified
# against (see docs/m15_ha_integration.md's test section). Not pinned to
# exact versions deliberately -- this mirrors ha-grohe_smarthome's own
# CI approach of testing against current HA, not a frozen snapshot -- but
# the versions actually used for the recorded test run were:
# aiohttp 3.14.3, pytest 9.0.3/9.1.1, pytest-asyncio 1.4.0,
# homeassistant 2026.9.1, pytest-homeassistant-custom-component 0.13.364.
aiohttp
pytest
pytest-asyncio
homeassistant
pytest-homeassistant-custom-component
